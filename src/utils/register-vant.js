// 注册引用vant组件
import Vue from 'vue'
import { Button, NavBar, Field, Cell, CellGroup, Toast, CountDown } from 'vant'
Vue.use(Button)
  .use(NavBar)
  .use(Field)
  .use(Cell)
  .use(CellGroup)
  .use(Toast)
  .use(CountDown)
